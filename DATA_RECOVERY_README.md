# 数据丢失问题修复说明

## 问题分析

MessageStats和EmojiStats插件存在数据丢失问题，主要原因包括：

1. **频繁保存导致的数据竞争**：每次收到消息都会立即保存，造成频繁的文件写入操作
2. **没有原子性写入**：直接覆盖原文件，写入过程中系统崩溃会导致数据丢失
3. **没有备份机制**：没有临时文件或备份文件来保证数据安全
4. **异常处理不当**：保存失败时直接抛出异常，可能导致数据丢失

## 修复方案

### 1. 原子性写入机制

- 使用临时文件先写入数据
- 强制刷新到磁盘 (`os.fsync`)
- 创建备份文件
- 原子性移动文件 (`shutil.move`)
- 写入成功后删除备份文件

### 2. 频率控制机制

- 30秒内只保存一次数据
- 使用线程锁防止并发保存
- 延迟保存机制，避免频繁写入

### 3. 备份恢复机制

- 每次保存前创建备份文件
- 加载失败时自动从备份恢复
- 提供手动恢复工具

### 4. 数据状态监控

- 提供数据状态检查方法
- 强制保存功能
- 备份文件清理功能

## 使用方法

### 自动恢复

插件启动时会自动检查数据完整性，如果发现主文件损坏会自动从备份恢复。

### 手动恢复

运行数据恢复工具：

```bash
python data_recovery_tool.py
```

### 插件内部方法

```python
# 获取数据状态
status = plugin.get_data_status()

# 强制保存
plugin.force_save()

# 清理旧备份（7天前）
plugin.cleanup_old_backups(7)
```

## 文件结构

```
data/json/
├── message_group_stats.json          # 发言统计群组数据
├── message_group_stats.json.backup   # 备份文件
├── message_user_stats.json           # 发言统计用户数据
├── message_user_stats.json.backup    # 备份文件
├── emoji_group_stats.json            # 表情包统计群组数据
├── emoji_group_stats.json.backup     # 备份文件
├── emoji_user_stats.json             # 表情包统计用户数据
└── emoji_user_stats.json.backup      # 备份文件
```

## 安全特性

1. **原子性写入**：确保写入过程中系统崩溃不会丢失数据
2. **自动备份**：每次保存前自动创建备份
3. **自动恢复**：加载失败时自动从备份恢复
4. **频率控制**：避免频繁写入导致的性能问题
5. **线程安全**：使用锁机制防止并发写入

## 监控建议

1. 定期检查日志中的保存状态
2. 监控备份文件的存在和大小
3. 定期运行数据恢复工具检查数据完整性
4. 设置定期清理旧备份文件的计划任务

## 注意事项

1. 备份文件会占用额外磁盘空间，建议定期清理
2. 如果同时存在主文件和备份文件，优先使用主文件
3. 恢复工具会自动处理文件损坏的情况
4. 建议在系统重启前调用 `force_save()` 确保数据保存

## 故障排除

### 数据丢失

1. 检查备份文件是否存在
2. 运行数据恢复工具
3. 查看插件日志中的错误信息

### 性能问题

1. 调整 `_save_interval` 参数（默认30秒）
2. 检查磁盘空间是否充足
3. 监控文件大小增长情况

### 权限问题

1. 确保插件有读写数据目录的权限
2. 检查临时文件目录的权限
3. 确保备份文件目录可写 